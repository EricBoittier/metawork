[package]
name = "madcore-scan"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dlpk = { version = "0.4.1", features = ["ndarray"] }
flate2 = { version = "1", default-features = false, features = ["zlib-rs"] }
metatensor = "0.5.1"
ndarray = "0.17"

[profile.release]
debug = false
